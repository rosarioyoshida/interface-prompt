import { test } from "@playwright/test"

test.describe.skip("History first prompt title", () => {
  test("uses first successful prompt as history title", async () => {
    // Scenario placeholder for full E2E with backend fixture data.
  })
})
