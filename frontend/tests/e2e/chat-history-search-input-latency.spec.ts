import { test } from "@playwright/test"

test.describe.skip("Chat search input latency", () => {
  test("captures keypress-to-render latency", async () => {
    // Placeholder for latency measurement scenario (NFR-003).
  })
})
