import { test } from "@playwright/test"

test.describe.skip("Chat search query and open", () => {
  test("searches term and opens selected conversation", async () => {
    // Placeholder for end-to-end query and navigation flow.
  })
})
