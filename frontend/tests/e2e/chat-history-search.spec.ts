import { test } from "@playwright/test"

test.describe.skip("Chat history search baseline", () => {
  test("baseline scenario", async () => {
    // Placeholder for CI environments without seeded backend data.
  })
})
