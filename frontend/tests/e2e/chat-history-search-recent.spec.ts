import { test } from "@playwright/test"

test.describe.skip("Chat search recent mode", () => {
  test("shows last 7 days when query is empty", async () => {
    // Placeholder for seeded recent-history assertions.
  })
})
