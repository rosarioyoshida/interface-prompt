import { test } from "@playwright/test"

test.describe.skip("History sidebar persistence", () => {
  test("keeps history after page reload", async () => {
    // Scenario placeholder for full E2E with backend fixture data.
  })
})
