import { test } from "@playwright/test"

test.describe.skip("Chat search open dialog", () => {
  test("opens search dialog from sidebar action", async () => {
    // Placeholder for full browser flow with fixture conversations.
  })
})
