import { test } from "@playwright/test"

test.describe.skip("History sidebar selection", () => {
  test("selects previous iteration", async () => {
    // Scenario placeholder for full E2E with backend fixture data.
  })
})
