import { test } from "@playwright/test"

test.describe.skip("History sidebar collapse", () => {
  test("collapse and expand without blocking prompt input", async () => {
    // Scenario placeholder for full E2E with backend fixture data.
  })
})
